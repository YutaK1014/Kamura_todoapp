# 画面設計書

## 1. 文書の目的

この文書は、新しく作るTodo（やること）管理アプリの6画面について、画面に表示する内容、入力方法、ボタン名、エラー文、画面遷移（画面から別の画面へ移る流れ）を決めるものです。既存のトップページを含めると、全体で7画面です。

トップページ `/` は既存のものをそのまま使うため、この文書では新しく設計しません。ログイン画面も作りません。

## 2. 画面一覧

| 画面番号 | 画面名 | アドレス（URL、画面の場所） | 役割 |
|---|---|---|---|
| 1 | 一覧 | `/todos` | 登録済みTodoの表示、絞り込み、並び替え、編集・削除への移動 |
| 2 | 登録 | `/todos/new` | 新しいTodoの入力 |
| 3 | 登録の確認 | `/todos/confirm` | 登録前に入力内容を確認 |
| 4 | 編集 | `/todos/{id}/edit` | 指定したTodoの修正。`{id}` はTodoを見分ける番号 |
| 5 | 編集の確認 | `/todos/{id}/confirm` | 保存前に修正内容を確認 |
| 6 | 削除確認 | `/todos/{id}/delete` | 削除するTodoを確認 |

## 3. 共通の画面ルール

### 3.1 表示と保存の対応

データベース（データを保存する仕組み）に保存する値と、画面に表示する言葉は次のように変換します。

| 保存する値 | 画面に表示する言葉 |
|---|---|
| 優先度 `1` | 高 |
| 優先度 `2` | 中 |
| 優先度 `3` | 低 |
| 完了状態 `1` | 完了 |
| 完了状態 `0` | 未完了 |

ジャンルは次の5種類から選びます。自由に文字を入力する欄にはしません。

`デザイン` ／ `マーケティング` ／ `プログラミング` ／ `資格` ／ `就職活動`

`id`（Todoを見分ける番号）、`created_at`（登録日時）、`updated_at`（最後に直した日時）は画面の入力欄に出しません。これらはデータベースが自動で設定します。

### 3.2 共通の見た目

6画面すべてに、画面全体の入れ物 `container` と画面タイトル `page-title` を使います。ボタンは `btn`、副ボタン（一覧へ、書き直す、やめる）は `btn sub`、文字リンクは `link` を使います。

CSS（画面の色や配置を決める仕組み）は、別途決められた1枚の `style.css` を6画面で読み込みます。この文書では新しい見た目や部品名を追加しません。

### 3.3 共通の入力チェック

入力チェック（入力内容が正しいかを確認する処理）で問題が見つかった場合、保存せず、入力画面にとどまります。該当する入力欄の直下に `error`（エラー文の表示場所）で文を表示します。

| 入力欄 | 条件 | 表示するエラー文 |
|---|---|---|
| やること | 未入力、または半角・全角の空白だけ | やることを入力してください |
| やること | 255文字を超えた | やることは255文字以内で入力してください |
| メモ | 255文字を超えた | メモは255文字以内で入力してください |
| ジャンル | 未選択 | ジャンルを選んでください |
| 優先度 | 未選択 | 優先度を選んでください |

期限は未入力でもよく、入力内容のチェックは行いません。

### 3.4 操作後の表示

登録・編集・削除の完了後、一覧画面の最上部に `flash`（直前の操作結果を表示する1行）を表示します。

| 操作 | 表示する文 |
|---|---|
| 登録完了 | 登録しました |
| 編集の保存完了 | 保存しました |
| 削除完了 | 削除しました |
| 存在しない番号を指定 | 見つかりませんでした |

編集または削除確認で存在しない`id`を指定した場合、専用のエラー画面は作らず、一覧へ戻して「見つかりませんでした」を表示します。

## 4. 画面詳細

### 4.1 画面1：一覧

#### 基本情報

- アドレス：`/todos`
- 画面タイトル：`Todo一覧`
- 使用する主な部品：絞り込み欄、並び替えリンク、Todo表、操作結果、ボタン

#### 画面に表示する内容

画面上部から次の順に表示します。

1. 直前の操作結果（該当する場合だけ）
2. 絞り込み欄
3. 並び替えリンク
4. `新規登録` ボタン
5. Todo一覧表

絞り込み欄には次を表示します。

| 表示する言葉 | 形 | 内容 |
|---|---|---|
| やること | 1行の入力欄 | Todo名の一部を入力 |
| ジャンル | 選ぶ欄 | `すべて`、5種類のジャンル |
|  | ボタン | `絞り込む` |

並び替えリンク（文字を押して順番を変えるリンク）は次の2本です。

- `期限が近い順`
- `期限が遠い順`

一覧表の見出しは左から次のとおりです。

`やること` ／ `ジャンル` ／ `優先度` ／ `期限` ／ `状態` ／ `操作`

各Todo行には次を表示します。

- やること、ジャンル、優先度（高・中・低）、期限、状態（完了・未完了）
- `編集` リンク
- `削除` リンク

登録が0件、または絞り込み結果が0件の場合は、表の代わりに次の文を表示します。

`まだ1件も登録されていません`

#### 動作と遷移

- `新規登録` → 登録画面 `/todos/new`
- 行の `編集` → 編集画面 `/todos/{id}/edit`
- 行の `削除` → 削除確認画面 `/todos/{id}/delete`
- `絞り込む` → 同じ一覧画面。条件をURLの後ろに付けます（例：`/todos?keyword=課題&category=プログラミング&order=asc`）
- `期限が近い順` → 同じ一覧画面を期限の近い順で表示
- `期限が遠い順` → 同じ一覧画面を期限の遠い順で表示

初めて開いたときの並び順は `期限が近い順` です。未入力の期限は、並び替えの向きにかかわらず最後に表示します。絞り込みは、やることが一部一致、ジャンルが完全一致の場合に該当するTodoを残します。「すべて」はジャンル条件を付けません。

### 4.2 画面2：登録

#### 基本情報

- アドレス：`/todos/new`
- 画面タイトル：`Todo登録`
- 入力欄の部品：`form-row`、ラベル `form-label`、入力欄 `form-input`

#### 画面に表示する内容

| ラベル | 入力方法 | 初期表示 | 必須か |
|---|---|---|---|
| やること | 1行の文字入力 | 空欄 | 必須 |
| メモ | 1行の文字入力 | 空欄 | 任意 |
| ジャンル | 5種類から選ぶ | 空欄 | 必須 |
| 優先度 | `高`・`中`・`低`から選ぶ | 空欄 | 必須 |
| 期限 | 日付入力 | 空欄 | 任意 |

ボタン名は `確認する` と `一覧へ` です。

#### 動作と遷移

- `確認する` → 入力チェック成功時は登録の確認画面 `/todos/confirm`
- 入力チェック失敗時 → この画面にとどまり、該当欄の下にエラー文を表示
- `一覧へ` → 一覧画面 `/todos`

### 4.3 画面3：登録の確認

#### 基本情報

- アドレス：`/todos/confirm`
- 画面タイトル：`Todo登録の確認`

#### 画面に表示する内容

登録画面で入力した次の5項目を、入力欄ではなく文字として表示します。

`やること` ／ `メモ` ／ `ジャンル` ／ `優先度` ／ `期限`

未入力の項目は、値の代わりに次の文を表示します。

`（入力なし）`

ボタン名は `登録する` と `書き直す` です。

#### 動作と遷移

- `登録する` → Todoを保存し、一覧 `/todos` へ移動して「登録しました」を表示
- `書き直す` → 入力内容を保持したまま登録画面 `/todos/new` へ戻る

### 4.4 画面4：編集

#### 基本情報

- アドレス：`/todos/{id}/edit`
- 画面タイトル：`Todo編集`

#### 画面に表示する内容

対象Todoの現在の値を、開いた時点で入力欄に入れて表示します。

| ラベル | 入力方法 | 必須か |
|---|---|---|
| やること | 1行の文字入力 | 必須 |
| メモ | 1行の文字入力 | 任意 |
| ジャンル | 5種類から選ぶ | 必須 |
| 優先度 | `高`・`中`・`低`から選ぶ | 必須 |
| 期限 | 日付入力 | 任意 |
| 完了 | チェックを入れる欄 | 必須（未チェックも値） |

完了状態は、未完了ならチェックなし、完了ならチェックありで表示します。ボタン名は `確認する` と `一覧へ` です。

#### 動作と遷移

- `確認する` → 入力チェック成功時は編集の確認画面 `/todos/{id}/confirm`
- 入力チェック失敗時 → この画面にとどまり、該当欄の下にエラー文を表示
- `一覧へ` → 一覧画面 `/todos`
- 対象`id`が存在しない → 一覧 `/todos` へ戻り、「見つかりませんでした」を表示

### 4.5 画面5：編集の確認

#### 基本情報

- アドレス：`/todos/{id}/confirm`
- 画面タイトル：`Todo編集の確認`

#### 画面に表示する内容

編集画面で入力した次の6項目を、入力欄ではなく文字として表示します。

`やること` ／ `メモ` ／ `ジャンル` ／ `優先度` ／ `期限` ／ `完了`

未入力の項目は `（入力なし）` と表示します。完了は `完了` または `未完了` と表示します。

ボタン名は `保存する` と `書き直す` です。

#### 動作と遷移

- `保存する` → Todoを更新し、一覧 `/todos` へ移動して「保存しました」を表示
- `書き直す` → 入力内容を保持したまま編集画面 `/todos/{id}/edit` へ戻る
- 対象`id`が存在しない → 一覧 `/todos` へ戻り、「見つかりませんでした」を表示

### 4.6 画面6：削除確認

#### 基本情報

- アドレス：`/todos/{id}/delete`
- 画面タイトル：`Todo削除の確認`

#### 画面に表示する内容

削除対象の次の3項目を、入力欄なしで文字として表示します。

`やること` ／ `ジャンル` ／ `期限`

期限が未入力の場合は `（入力なし）` と表示します。

確認文として次を表示します。

`このTodoを削除しますか？`

ボタン名は `消す` と `やめる` です。

#### 動作と遷移

- `消す` → Todoを削除し、一覧 `/todos` へ移動して「削除しました」を表示
- `やめる` → 削除せず一覧 `/todos` へ戻る
- 対象`id`が存在しない → 一覧 `/todos` へ戻り、「見つかりませんでした」を表示

## 5. 画面遷移の全体図

```
一覧 ──「新規登録」──→ 登録 ──「確認する」──→ 登録の確認 ──「登録する」──→ 一覧
  ↑                      ↑                         │
  │                      └────「書き直す」─────────┘
  │
  ├── 行の「編集」────→ 編集 ──「確認する」──→ 編集の確認 ──「保存する」──→ 一覧
  │                      ↑                         │
  │                      └────「書き直す」─────────┘
  │
  └── 行の「削除」────→ 削除確認 ──「消す」──→ 一覧
                              └──「やめる」──→ 一覧
```

一覧画面の絞り込みと並び替えは、一覧画面の中で表示内容だけを変えます。登録・編集・削除が完了したときは、一覧画面の上部に操作結果を1行表示します。

## 6. 画面部品名の対応

画面部品（画面を構成する各部分）には、決められた次の名前だけを使います。

| 部品 | 使用する名前 | 使用画面 |
|---|---|---|
| 画面全体の入れ物 | `container` | 6画面すべて |
| 画面タイトル | `page-title` | 6画面すべて |
| Todo一覧表 | `todo-table` | 一覧 |
| 操作結果 | `flash` | 一覧 |
| 絞り込み・並び替えの帯 | `search-bar` | 一覧 |
| ラベルと入力欄の組 | `form-row` | 登録・編集 |
| ラベル | `form-label` | 登録・編集 |
| 入力欄・選択欄 | `form-input` | 登録・編集 |
| エラー文 | `error` | 登録・編集 |
| 確認画面の表示値 | `confirm-value` | 登録の確認・編集の確認・削除確認 |
| 確認画面のボタンの並び | `confirm-actions` | 登録の確認・編集の確認・削除確認 |
| ボタン | `btn` | 6画面すべて |
| 副ボタン | `btn sub` | 一覧へ・書き直す・やめる |
| 文字リンク | `link` | 編集・削除・並び替え |
