# 要件定義書

## 1. 文書の目的

本書は、やること（以降「Todo」と呼びます）を登録・管理するアプリの要件をまとめたものです。

DB（データを保存する仕組み）の詳しい設計書と、画面の詳しい設計書は別に作成します。本書では、それらの設計に必要な「何ができる必要があるか」を定めます。

## 2. 対象範囲

### 2.1 作成するもの

次の6つの機能を持つTodo管理アプリを作成します。

| 機能 | 内容 |
|---|---|
| 一覧 | 登録したTodoを1画面に表示する |
| 登録 | 新しいTodoを1件登録する |
| 編集 | 登録済みのTodoを修正する |
| 削除 | 登録済みのTodoを削除する |
| 検索 | Todoの名前で一覧を絞り込む |
| 並び替え | 期限の順に一覧を並べ替える |

### 2.2 利用者

利用者は1人です。ログイン機能は作成しません。

### 2.3 作成しないもの

次の機能は今回の対象外です。

- 一覧のページ分け（件数が多いときに複数ページへ分ける仕組み）
- 削除したTodoを元に戻す機能
- Todoを完了にした日時の記録
- ログイン機能、複数利用者での共有
- 写真やファイルの添付

## 3. システムの前提

- アプリは `http://localhost:8080` で利用します。
- データはMySQL（データベース製品）に保存します。
- 保存先のテーブル（データを表形式で保存する場所）は `todos` という名前にします。
- `todos` テーブルでは、1件のTodoを1行で管理します。

## 4. 機能要件

### 4.1 Todoの一覧表示

- 登録済みのTodoを一覧で表示します。
- 表示する項目は、左から「やること」「ジャンル」「優先度」「期限」「状態」「操作（編集・削除）」です。
- 1件も登録されていない場合、または絞り込み結果が0件の場合は、「まだ1件も登録されていません」と表示します。
- 一覧画面の上部に、直前の操作結果を1行で表示します。

### 4.2 Todoの登録

- 新しいTodoを1件入力できます。
- 登録前に確認画面を表示します。
- 確認画面では入力欄ではなく、入力内容を文字として表示します。
- 入力していない項目は「（入力なし）」と表示します。
- 「登録する」を選ぶと保存して一覧へ戻り、「登録しました」と表示します。
- 「書き直す」を選ぶと、入力内容を保持したまま登録画面へ戻ります。

### 4.3 Todoの編集

- 一覧の各行から、対象Todoの編集画面を開けます。
- 編集画面を開いたときは、現在の値を入力欄に表示します。
- 登録時の項目に加えて、「完了（チェックを入れる欄）」を編集できます。
- 保存前に確認画面を表示します。
- 「保存する」を選ぶと保存して一覧へ戻り、「保存しました」と表示します。
- 「書き直す」を選ぶと、入力内容を保持したまま編集画面へ戻ります。

### 4.4 Todoの削除

- 一覧の各行から、対象Todoの削除確認画面を開けます。
- 削除確認画面には、対象Todoの「やること」「ジャンル」「期限」を表示します。
- 削除確認画面に入力欄は置きません。
- 「消す」を選ぶと削除して一覧へ戻り、「削除しました」と表示します。
- 「やめる」を選ぶと削除せず一覧へ戻ります。

### 4.5 Todoの検索・絞り込み

- Todoの名前を入力して絞り込めます。
- 名前は `title` に入力した言葉が一部でも含まれるTodoを残します。
- ジャンルを選んで絞り込めます。
- ジャンルは `category` が選択値と完全に一致するTodoだけを残します。
- ジャンルの「すべて」を選ぶと、ジャンルでは絞り込みません。
- 名前とジャンルがどちらも空、または「すべて」の場合は、全件を表示します。
- 「絞り込む」ボタンで絞り込みます。
- 絞り込み条件はアドレス（URL）の後ろに付けて送ります。例：`/todos?keyword=課題&category=プログラミング&order=asc`

### 4.6 Todoの並び替え

- 一覧に「期限が近い順」と「期限が遠い順」のリンクを表示します。
- 最初に一覧を開いたときは、期限が近い順に表示します。
- 期限が未入力のTodoは、並び替えの向きにかかわらず最後に表示します。
- 並び替えの向きはアドレス（URL）の後ろに付けて送ります。

## 5. データ要件

### 5.1 Todoの保存項目

| 項目 | 保存内容 | データ型 | 必須・任意 | 初期値・自動設定 |
|---|---|---|---|---|
| `id` | Todoを見分ける番号 | `BIGINT` | 必須 | 自動で1ずつ増える。主キー（1件を特定する番号） |
| `title` | やること | `VARCHAR(255)` | 必須 | なし |
| `detail` | メモ | `VARCHAR(255)` | 任意 | なし |
| `category` | ジャンル | `VARCHAR(255)` | 必須 | なし |
| `priority` | 優先度。1=高、2=中、3=低 | `INT` | 必須 | 2（中） |
| `due_date` | 期限 | `DATE` | 任意 | なし |
| `completed` | 完了したかどうか | `BOOLEAN` | 必須 | 未完了 |
| `created_at` | 登録日時 | `DATETIME` | 必須 | MySQLが自動設定 |
| `updated_at` | 最終更新日時 | `DATETIME` | 必須 | MySQLが自動設定 |

### 5.2 ジャンル

`category` に保存できるジャンルは、次の5つです。自由入力ではなく、選択式にします。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

### 5.3 利用者が入力しない項目

`id`、`created_at`、`updated_at` は画面の入力欄に表示しません。保存時に自動で値を設定します。

## 6. 画面要件

新しく作るTodo関連画面は6画面です。既存のトップページを含めると、全体で7画面です。

### 6.1 画面一覧

| 画面 | アドレス | 役割 |
|---|---|---|
| トップページ | `/` | 既存のトップページをそのまま使用する |
| 一覧 | `/todos` | Todoの表示、検索、並び替え、各操作への移動 |
| 登録 | `/todos/new` | 新しいTodoの入力 |
| 登録の確認 | `/todos/confirm` | 登録内容の確認 |
| 編集 | `/todos/{id}/edit` | 指定したTodoの修正 |
| 編集の確認 | `/todos/{id}/confirm` | 修正内容の確認 |
| 削除確認 | `/todos/{id}/delete` | 削除対象の確認 |

`{id}` には、対象Todoの番号が入ります。

### 6.2 画面遷移

（画面遷移とは、画面から別の画面へ移動する流れです。）

- 一覧 →「新規登録」→ 登録 →「確認する」→ 登録の確認 →「登録する」→ 一覧
- 登録の確認 →「書き直す」→ 登録
- 一覧 → 行の「編集」→ 編集 →「確認する」→ 編集の確認 →「保存する」→ 一覧
- 編集の確認 →「書き直す」→ 編集
- 一覧 → 行の「削除」→ 削除確認 →「消す」→ 一覧
- 削除確認 →「やめる」→ 一覧
- 登録・編集画面の「一覧へ」→ 一覧

## 7. 入力・エラー要件

必須項目（必ず入力する項目）が空の場合や、文字数を超えた場合は保存しません。入力画面に留まり、該当項目の下に次の文を表示します。

| 項目 | 条件 | 表示する文 |
|---|---|---|
| やること | 空のまま | やることを入力してください |
| やること | 255文字を超える | やることは255文字以内で入力してください |
| メモ | 255文字を超える | メモは255文字以内で入力してください |
| ジャンル | 未選択 | ジャンルを選んでください |
| 優先度 | 未選択 | 優先度を選んでください |

- 空白だけの入力も空と扱います。半角スペースと全角スペースの両方を対象にします。
- 期限には入力上の制限を設けません。
- 登録画面の入力項目は「やること」「メモ」「ジャンル」「優先度」「期限」です。
- 編集画面の入力項目は、登録画面の5項目に「完了」を加えた6項目です。
- 登録時に「ジャンル」と「優先度」の初期表示は空欄です。

## 8. 存在しないTodoの扱い

編集または削除確認のアドレスに、保存されていない番号を指定した場合は、専用のエラー画面を作りません。一覧へ戻し、画面上部に「見つかりませんでした」と表示します。

## 9. 表示上の変換

保存値と、画面に表示する言葉は次のように対応させます。

| 保存値 | 画面表示 |
|---|---|
| `priority` の `1` | 高 |
| `priority` の `2` | 中 |
| `priority` の `3` | 低 |
| `completed` の `1` | 完了 |
| `completed` の `0` | 未完了 |

## 10. 操作記録（ログ）要件

登録・編集・削除が実行されたとき、1操作につき1行のログ（操作の記録）を残します。

- 記録する内容：操作の種類（登録・編集・削除）と対象Todoの `id`
- 記録しない内容：`title` と `detail` の中身

## 11. 見た目・CSS要件

CSS（画面の色や配置を決める仕組み）は1枚だけ作成し、6枚の画面すべてで読み込みます。画面ごとに見た目を変えません。

- ファイルの場所：`src/main/resources/static/css/style.css`
- 各テンプレートの `<head>` に、`<link rel="stylesheet" href="/css/style.css">` を1行置きます。
- 画面内に直接書く `style="..."` は使いません。
- CSSファイルを2枚目以降は作りません。
- 影、動き、画像、外部から読み込むCSSは使いません。

### 11.1 使用するclass名

class（画面の部品に付ける名前）は、次の名前だけを使用します。

| 用途 | class名 | 使用画面 |
|---|---|---|
| 画面全体の入れ物 | `container` | 6枚すべて |
| 画面タイトル | `page-title` | 6枚すべて |
| 一覧表 | `todo-table` | 一覧 |
| 操作結果の表示 | `flash` | 一覧 |
| 検索・並び替えの帯 | `search-bar` | 一覧 |
| ラベルと入力欄の組 | `form-row` | 登録・編集 |
| ラベル | `form-label` | 登録・編集 |
| 入力欄・選択欄 | `form-input` | 登録・編集 |
| エラー文 | `error` | 登録・編集 |
| 確認画面の値 | `confirm-value` | 登録の確認・編集の確認・削除確認 |
| 確認画面のボタンの並び | `confirm-actions` | 登録の確認・編集の確認・削除確認 |
| ボタン | `btn` | 6枚すべて |
| 副ボタン（書き直す・やめる・一覧へ） | `btn sub` | 6枚すべて |
| 文字リンク（編集・削除・並び替え） | `link` | 一覧 |

CSSの具体的な記述内容は、叩き台の「6-3. CSS」に記載された内容をそのまま要件とします。

## 12. 追加事項

本書では、叩き台に書かれていない機能要件・データ項目・画面項目を追加していません。

「文書の目的」「対象範囲」「システムの前提」などの見出しは、叩き台の内容を要件定義書として読みやすく整理するために追加した構成です。新しい機能や動作を追加するものではありません。
