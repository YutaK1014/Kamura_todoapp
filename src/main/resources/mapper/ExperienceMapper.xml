<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todoapp.ExperienceMapper">
    <select id="getTotalPoints" resultType="int">SELECT total_points FROM player_status WHERE id = 1</select>
    <update id="addPoints">UPDATE player_status SET total_points = total_points + #{points} WHERE id = 1</update>
</mapper>
